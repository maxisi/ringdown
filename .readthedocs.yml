version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"

# conda:
#   environment: docs/environment.yml

sphinx:
  configuration: docs/conf.py

# formats: all

python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements_docs.txt

submodules:
  include: all
